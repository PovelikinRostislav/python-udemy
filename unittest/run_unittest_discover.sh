echo "This script shows how to run tests using unittest discovery (files with 'test_*')"
echo "But be sure that unttest searches only for regular packages (not namespace packages)"
python -m unittest
